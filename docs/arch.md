\# Architecture (ProofLens)

\- Browser verifier (caption-mode)

\- Manifest schema (JSON)

\- Signers: client (WebCrypto) + hosted (serverless)

\- Future: WP Auto-CR plugin, Edge auto-manifest, SW caching



